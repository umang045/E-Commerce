import React from "react";
import {ClockLoader} from "react-spinners";

const Loading = () => {
  return (
    <ClockLoader color="#171195" cssOverride={{}} loading speedMultiplier={1} />
  );
};

export default Loading;
